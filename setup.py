from __future__ import print_function, division, absolute_import

from distutils.core import setup

version = "0.0.1"

setup(name="tsfc",
      version=version,
      packages=["gem", "tsfc", "tsfc.kernel_interface"])
